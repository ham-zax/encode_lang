# Historical V2 material

These files preserve the pre-field V2 bootstrap, corpus, pilot evidence and selected documentation from the baseline at Git commit `38b12de`. They are not active 2.1 instructions or fallback decoders.

The bootstrap/corpus/result files at this archive root retain their original contents. Documentation links may describe the original repository-root layout; use the historical Git revision for that complete layout and runtime. The active entry point is `../../prompt/BOOTSTRAP.md`.

The recorded V2 pilot is not evidence about numeric 2.1 transport, directional fields, or the new prompt. Do not update its hashes or labels to make it appear current.
